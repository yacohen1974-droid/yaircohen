# תוכנית הצלה ל‑CMS

מטרה: CMS פשוט, יציב ובטוח — כמו באתר מסחרי מוביל. בלי "דחיפות", בלי כפילויות, בלי הפתעות.

מסמך זה כתוב לפי מצב הקוד ב‑`e0b7725`. כל טענה כאן אומתה מול הקוד/הנתונים בפועל.

---

## 0. מה קרה עכשיו (אירוע חי שכבר טופל)

בזמן העבודה על המסמך הזה, ה‑repo נתקע באמצע `git rebase` עם קונפליקט ב‑`src/content/site-data.json`, והקובץ הכיל סמני קונפליקט (`<<<<<<<`) — כלומר **הוא לא היה JSON תקין**.

- הסיבה: ה‑CMS עושה `git commit` אוטומטי ל‑`main` בכל פרסום ("admin: publish website updates via CMS"), במקביל לעבודת פיתוח מקומית. שני הזרמים נלחמים על אותו קובץ.
- הפתרון שבוצע: נשמרו גיבויים (`pre-rescue-backup`, `pre-rescue-upstream`, ושתי גרסאות JSON בתיקיית scratchpad), נבחרה גרסת השרת (`about-me` — התוכן החי שלך), ה‑rebase הושלם, `tsc` עובר נקי.
- **זו לא תקלה חד־פעמית. זו תוצאה ישירה של הארכיטקטורה** (סעיף 1.3). כל עוד היא לא משתנה, זה יקרה שוב.

> אם קובץ ה‑JSON נשבר שוב: `git status` → אם יש `UU` על site-data.json, שחזר מגרסת השרת ב‑`git checkout --ours src/content/site-data.json`.

---

## 1. הבעיות השורשיות

### 1.1 שלושה־ארבעה "מקורות אמת" מתחרים
| מקור | מי כותב | מי קורא |
|---|---|---|
| `localStorage` (טיוטה) | העורך, אוטוסייב | הבאנר, העורך, האתר החי בדפדפן שלך |
| `src/content/site-data.json` על הדיסק | `publish/route.ts:26`, `db-actions.ts:26` | `getDbInitialData`, `list-pages`, `sitemap`, `middleware` |
| GitHub `main` | publish (commit) | `publish-status` |
| GitHub `draft` (ענף נפרד) | `save-draft` | `preview-draft` |

אין מקור אמת אחד. כל באג "מראה מצב שגוי" נולד מהפער בין השניים.

### 1.2 כתיבה לדיסק היא חסרת משמעות בפרודקשן
`apphosting.yaml` מגדיר `maxInstances: 3`. כל אינסטנס עם דיסק **זמני ונפרד**. לכן `fs.writeFile` על `site-data.json`:
- או נכשל בשקט (עטוף ב‑`try/catch` עם `console.warn` בלבד),
- או נוחת על אינסטנס אחד מתוך שלושה.

זה בדיוק מה שגרם לבאג "תוכן שלא נדחף למרות שנדחף": בדיקת הסטטוס פנתה לדיסק המקומי וקיבלה תשובה מאינסטנס אקראי. (כבר תוקן — `publish-status` פונה ל‑GitHub.)

### 1.3 "פרסום" = commit ל‑main = דיפלוי מלא
זו הסיבה ל‑2 דקות המתנה, לבאנר ה"מפחיד", ולקונפליקטים מול פיתוח. CMS מסחרי לא מדפלוי את האתר כדי לשנות כותרת.

### 1.4 מבנה נתונים כפול
קיים גם `data.pages[id]` וגם מבנה ישן `data[id]` ברמה העליונה. שני נתיבי הקריאה תומכים בשניהם:
- `db-actions.ts:92` — `data.pages?.[pageId] || data[pageId]`
- `admin/pages/page.tsx:1608` — `draft.pages?.[id] || draft[id]`

בפועל, `site-data.json` **עדיין מכיל `home` כפול** — גם ב‑`pages.home` וגם ברמה העליונה.

### 1.5 הגדרות גלובליות נדבקות לכל עמוד
`getInitialPageContent()` זורע 15 שדות גלובליים (`siteName`, `siteLogo`, `navItems`...) על **כל** עמוד, ואז האוטוסייב שומר אותם לתוך `pages[id]`. התוצאה: הטיוטה תמיד שונה מהפרסום → הבאנר תמיד צועק "יש שינויים".

תוקן בקוד (`GLOBAL_ONLY_CONTENT_FIELDS`), אבל **הנתונים עדיין מזוהמים**: `pages.home` מכיל כרגע את כל 15 השדות.

### 1.6 יצירת עמוד חדש — מסורבל ושביר
- העמוד החדש הוא ערך קסם `"custom"` באותו dropdown של בחירת עמוד.
- התוכן **לא מאופס** — העמוד החדש יורש את התוכן של העמוד שהיה פתוח קודם (`page.tsx:1596-1598`).
- ה‑slug הוא טקסט חופשי, והסניטייזר הופך עברית לשרשרת מקפים — שני שמות עבריים שונים יכולים להתנגש לאותו מזהה ולדרוס זה את זה.

### 1.7 כפילות בין "יצירת עמוד" ל"הוספה לתפריט"
שתי פעולות נפרדות לגמרי, בלי קשר ביניהן. הקלדת href שגוי = קישור בתפריט שמצביע לשומקום. (שופר חלקית עם `PageLinkInput`, אבל התהליך עדיין דו‑שלבי.)

### 1.8 קוד מת ונפיחות
- `savePageContent`, `deletePageContent`, `saveBlogPost`, `deleteBlogPost` — מיוצאים, **אפס שימושים**.
- `sectionOrder` — נשמר בכל עמוד, **לא בשימוש באף רינדור**.
- `admin/pages/page.tsx` — **2,372 שורות**, 18 `useState`, בקומפוננטה אחת.

### 1.9 טיוטה חיה רק בדפדפן
האוטוסייב כותב ל‑`localStorage` בלבד. ניקוי היסטוריה / דפדפן אחר / מכשיר אחר = כל העבודה נעלמה.

---

## 2. תמונת היעד

**מקור אמת אחד: מסד נתונים (Firestore).** האתר קורא ממנו, ה‑CMS כותב אליו. שמירה = מיידית. פרסום = החלפת דגל `status: draft → published`, לא דיפלוי.

```
עורך → שמירה אוטומטית → Firestore (draft)
                              ↓ "פרסם" = דגל
                         Firestore (published) → האתר החי (ISR/revalidate)
```

- אין `fs.writeFile`. אין commit אוטומטי. אין קונפליקטים מול git.
- פרסום לוקח שניות, לא דקות.
- `site-data.json` הופך ל‑seed ראשוני בלבד.

Firestore כבר מותקן ומאותחל בפרויקט (`src/firebase/`) — זו לא הוספת תלות חדשה.

---

## 3. השלבים

### שלב 1 — ייצוב מיידי (ללא שינוי ארכיטקטורה)
מטרה: לעצור דימום. אפשר לעשות היום.

1. **מיגרציית נתונים חד־פעמית** על `site-data.json`:
   - הסרת 15 השדות הגלובליים מכל `pages[*]` (סקריפט קיים, צריך ריצה חוזרת אחרי ה‑rebase).
   - מחיקת המפתח הישן `home` מרמת העליון.
   - הסרת `sectionOrder`.
2. **הסרת קוד מת**: 4 הפונקציות מ‑`db-actions.ts`, נתיבי ה‑fallback ל‑`data[id]`.
3. **הסרת `fs.writeFile`** מ‑`publish/route.ts` ומ‑`db-actions.ts` — הן משקרות בפרודקשן. GitHub הוא המקור.
4. **נעילת הסכימה**: `pages[id]` בלבד. מבנה אחד, בלי fallbacks.

> תוצאה: הבאנר מפסיק לשקר, אין יותר עמודים "כפולים", הקוד קטן משמעותית. הארכיטקטורה עדיין git‑based.

### שלב 2 — מעבר ל‑Firestore (הלב)
5. סכימה: `sites/{siteId}/pages/{pageId}` עם `draft` ו‑`published`, ו‑`sites/{siteId}/global`.
6. Security Rules: קריאה ל‑published בלבד לציבור; כתיבה רק ל‑admin מאומת.
7. `save-draft` → כתיבה ל‑Firestore (במקום ענף git). מוחק את כל לוגיקת `DRAFT_BRANCH`.
8. `publish` → העתקת draft ל‑published + `revalidateTag`. **בלי commit, בלי דיפלוי.**
9. האתר החי קורא מ‑Firestore. `site-data.json` → `scripts/seed.ts` חד־פעמי.
10. תצוגה מקדימה = קריאת `draft` עם קוקי. מוחק את `preview-draft` + ניהול ענפים.

> תוצאה: נעלמים סעיפים 1.1, 1.2, 1.3, 1.9 במלואם. פרסום מיידי. אין קונפליקטי git לעולם.

### שלב 3 — פישוט חוויית העורך
11. **"עמוד חדש" = פעולה נפרדת**, לא ערך בדropdown. דיאלוג אחד:
    - שם העמוד (עברית, חופשי) → הצגה חיה של ה‑slug שייווצר.
    - צ'קבוקס: ☑ הוסף לתפריט הראשי.
    - התוכן מאותחל **ריק**, לא יורש מהעמוד הקודם.
    - יצירה → מעבר אוטומטי לעריכת העמוד.
    זה מבטל את 1.6 ואת 1.7 יחד — עמוד וקישור נולדים באותה פעולה.
12. **פיצול `admin/pages/page.tsx`** ל‑: `GlobalSettingsEditor`, `PageEditor`, `BlockEditor`, `PageSelector`. יעד: אף קובץ מעל ~400 שורות.
13. **איחוד מסכי הדפים**: מסך אחד "דפים" עם רשימה + עריכה + מחיקה. `manage-pages` נמחק.
14. **הורדת עומס**: אוטוסייב שקט (בלי באנר אזהרה קבוע), אינדיקטור "נשמר ✓" בלבד. הבאנר מופיע רק כשבאמת יש הבדל מול ה‑published.

### שלב 4 — רשת ביטחון
15. בדיקות ל‑slug, למיזוג draft/published, ולחישוב "יש שינויים".
16. ולידציה של הסכימה (zod) לפני כל כתיבה — כדי ש‑JSON שבור לא ייכנס לעולם.
17. גיבוי אוטומטי: snapshot של published לפני כל פרסום + "שחזור לגרסה קודמת" אמיתי.

---

## 4. סדר עדיפויות

| # | פעולה | ערך | סיכון | מתי |
|---|---|---|---|---|
| 1 | מיגרציית נתונים + הסרת קוד מת (שלב 1) | גבוה | נמוך | מיד |
| 2 | הסרת `fs.writeFile` | גבוה | נמוך | מיד |
| 3 | דיאלוג "עמוד חדש" מאוחד (11) | גבוה | נמוך | מיד |
| 4 | מעבר ל‑Firestore (שלב 2) | קריטי | בינוני | הצעד הגדול |
| 5 | פיצול הקומפוננטה (12–13) | בינוני | בינוני | אחרי Firestore |
| 6 | בדיקות + ולידציה (שלב 4) | גבוה | נמוך | לאורך הדרך |

**המלצה:** לבצע 1–3 עכשיו (יום עבודה, סיכון נמוך, שקט מיידי), ואז להיכנס ל‑Firestore כפרויקט ממוקד. פיצול הקומפוננטה **אחרי** Firestore — אין טעם לארגן מחדש קוד שעומד להימחק.

---

## 5. מה כבר תוקן בסבב הזה

- `publish-status` קורא מ‑GitHub ולא מדיסק מקומי → הבאנר לא משקר יותר.
- כפתור עריכה בכל דף ב‑`manage-pages` + תמיכה ב‑`?page=<id>`.
- `src/lib/cms-pages.ts` — מקור אחד לרשימת הדפים (במקום שתי מימושים כפולים).
- `src/lib/slug.ts` — מקור אחד לחישוב slug, כולל הסרת `/` מוביל.
- קישורי תפריט/פוטר = בחירה מרשימת דפים אמיתית (`PageLinkInput`), לא טקסט חופשי.
- `GLOBAL_ONLY_CONTENT_FIELDS` — שדות גלובליים לא נדבקים יותר לעמוד בודד. (`primaryColor` נשאר per‑page בכוונה.)

---

## 6. סיכונים

- **מיגרציה ל‑Firestore היא נקודת אל‑חזור.** לגבות `site-data.json` בתגית git ייעודית לפני.
- **Security Rules הן החלק הקריטי.** כתיבה פתוחה = כל אחד משנה את האתר. לבדוק לפני עלייה.
- **עלות Firestore** זניחה בנפח הזה, אך יש לוודא שהאתר החי לא קורא בכל בקשה (ISR/cache).
- `GITHUB_TOKEN` יושב ב‑`.env` שמוחרג כראוי מ‑git (אומת). אחרי המעבר ל‑Firestore הוא מיותר לחלוטין — להסיר ולבטל אותו.
